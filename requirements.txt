# ── Groq SDK (optional but recommended) ──────────────────────────────────────
groq>=0.9.0                     # Official Groq Python SDK (wraps the HTTP calls)
# Note: the raw HTTP calls in llm/orchestrator.py work without any extra SDK.
# Install groq only if you want the convenience client elsewhere in your code.

# MindBridge LLM — Dependencies
# ──────────────────────────────
# Install with: pip install -r requirements.txt
#
# Hardware requirements for production training:
#   - 8× A100 80GB GPUs (for 1.3B model)
#   - 64× A100 80GB or H100s (for 7B model)
#   - 500GB+ shared storage for checkpoints

# ── Core deep learning ────────────────────────────────────────────────────────
torch>=2.2.0                    # FSDP, Flash Attention 2, bf16 support
torchvision>=0.17.0             # Required by some torch utilities

# ── Tokenization ──────────────────────────────────────────────────────────────
tokenizers>=0.15.0              # HuggingFace tokenizers (BPE training)
transformers>=4.38.0            # For HF-compatible tokenizer wrapper

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0             # Metrics (AUC, F1 for evaluation)

# ── Training monitoring ───────────────────────────────────────────────────────
wandb>=0.16.0                   # Experiment tracking (optional but recommended)
tensorboard>=2.14.0             # Alternative logger

# ── Distributed training ──────────────────────────────────────────────────────
# torch already includes torch.distributed and FSDP
# No extra packages needed unless using DeepSpeed

# ── Inference (Phase 7) ───────────────────────────────────────────────────────
# vllm>=0.3.0                   # High-throughput serving (uncomment for Phase 7)
# auto-gptq>=0.6.0              # GPTQ quantization
# bitsandbytes>=0.41.0          # INT8/INT4 quantization via bnb

# ── Phase 3 fine-tuning (SFT / RLHF) ─────────────────────────────────────────
# peft>=0.8.0                   # LoRA adapters (uncomment for Phase 3)
# trl>=0.7.0                    # DPO / PPO training loop

# ── Safety (Phase 2) ─────────────────────────────────────────────────────────
# detoxify>=0.5.0               # Content safety scoring

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.65.0
rich>=13.0.0                    # Pretty terminal output
pyyaml>=6.0                     # Config files
click>=8.1.0                    # CLI scripts
