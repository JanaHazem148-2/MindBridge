# MindBridge — Full Requirements (Phase 8 + Auth)
#
# Install:
#   pip install -r requirements_full.txt
#
# On system Python (e.g. Ubuntu / Raspberry Pi) add the flag:
#   pip install -r requirements_full.txt --break-system-packages

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.8.0
bcrypt>=4.1.0        # secure password hashing (requires --break-system-packages on Ubuntu)

# ── LLM / AI ─────────────────────────────────────────────────────────────────
groq>=0.9.0
openai>=1.30.0
httpx>=0.27.0

# ── Data / Storage ────────────────────────────────────────────────────────────
# SQLite is built-in — no package needed for default setup
#
# PostgreSQL (optional — uncomment for production):
# asyncpg>=0.29.0
# psycopg2-binary>=2.9.9
#
# Redis cache (optional — uncomment to enable):
# redis[hiredis]>=5.0.0

# ── ML / Embeddings (for RAG) ─────────────────────────────────────────────────
numpy>=1.26.0
scikit-learn>=1.4.0
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0

# ── NLP / Safety ──────────────────────────────────────────────────────────────
langdetect>=1.0.9

# ── Dev utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
